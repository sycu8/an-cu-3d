# `@ancu/floorplan-engine`

Internal conversion service: allowlisted crawl → ingest → analyze → FloorPlanDocument → review/publish.

Scaffold only (P1). Pipeline work: P8–P23.

Planned bindings: `ENGINE_DB`, `FLOORPLANS`, `CONVERSION_QUEUE`, `CRAWL_QUEUE`, `CONVERSION_WORKFLOW`, `AI`, `BROWSER`.

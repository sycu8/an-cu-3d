# `@ancu/property-web-worker`

Public AnCư 3D experience: React + TypeScript UI, project/map/apartment APIs, Workers Assets.

Scaffold only (P1). Implementation starts P2 (tokens via shared) / P3 (wrangler) / P6 (routes).

Planned bindings: `DB`, `ASSETS`, optional `ENGINE`.

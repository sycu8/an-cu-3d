ENGINE_API_SECRET=
# Local-only admin Bearer secret (never commit real production secrets)
ADMIN_SECRET=local-admin-qa-secret
# Optional AI Gateway — leave blank for local UI without live model calls
# AI_GATEWAY_ACCOUNT_ID=
# AI_GATEWAY_ID=
# AI_GATEWAY_TOKEN=
# ENGINE_BASE_URL=
